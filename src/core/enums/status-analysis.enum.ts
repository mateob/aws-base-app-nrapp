/* eslint-disable no-unused-vars */
export enum StatusAnalysisEnum {
  PENDING = 'pending',
  IN_PROGRESS = 'in progress',
  ASSIGNED = 'assigned',
  FINISHED = 'finished',
  CANCELED = 'canceled',
  BLOQUED = 'bloqued'
}
