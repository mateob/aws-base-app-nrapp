export const VALUE_NOT_FOUND = (entity: string) => `${entity} not found.`;
export const ALREADY_PROCESSED = (entity: string, dateTime: string) => `${entity} already processed in ${dateTime}.`;
export const ALREADY_CANCELED = (entity: string, dateTime: string) => `${entity} already canceled in ${dateTime}.`;
