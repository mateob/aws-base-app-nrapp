export interface ISettings {
  readonly nodeEnv: string;
  readonly loggerLevel: string;
  readonly loggerEnabled: string;
  readonly databaseConnectionString: string;
  readonly awsRegion: string;
}
