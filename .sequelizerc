const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "config", "sequelize-cli-config.js"),
  "migrations-path": path.resolve(__dirname, "src", "config", "migrations"),
  "seeders-path": path.resolve(__dirname, "src", "config", "seeders"),
};
